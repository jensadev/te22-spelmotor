export default class Game {
  constructor(width, height) {
    this.width = width
    this.height = height
  }

  update(deltaTime) {
    // Update game state
  }

  draw(ctx) {
    // Draw game state
  }
}